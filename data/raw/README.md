# Raw data inputs

This study currently uses a **public real-data proxy pilot panel** as a benchmarking input:

- `real_proxy/repo_week_panel_pilot.csv`

Important caveat: this pilot panel is sparse and has no observed AI-adoption switches in the available window. The benchmarking script therefore applies explicitly documented placeholder assumptions for adoption timing moments.
